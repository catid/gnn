model:
  nodes_total: 32
  cache_capacity: 64
task:
  name: memory
  writers_per_episode: 6
  max_rollout_steps: 10
train:
  batch_size_per_gpu: 16
  train_steps: 100
  log_interval: 20
  eval_interval: 1000
  save_interval: 1000
  val_batches: 4
  lr: 2.0e-4
runtime:
  run_name: throughput-multigpu
