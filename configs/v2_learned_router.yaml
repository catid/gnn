model:
  nodes_total: 32
  cache_capacity: 64
  use_first_hop_key_hint: false
  use_learned_first_hop_router: true
task:
  name: memory
  writers_per_episode: 6
  max_rollout_steps: 10
train:
  batch_size_per_gpu: 16
  train_steps: 1500
  eval_interval: 150
  log_interval: 25
  save_interval: 150
  val_batches: 40
  lr: 2.0e-4
  aux_writer_weight: 2.0
  aux_query_weight: 2.0
  first_hop_teacher_force_start: 1.0
  first_hop_teacher_force_end: 0.0
  first_hop_teacher_force_anneal_steps: 900
runtime:
  run_name: v2-learned-router
